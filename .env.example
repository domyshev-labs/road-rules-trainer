ADDRESS=:8080
DATABASE_PATH=data/road-rules.db
STATIC_DIR=dist
SESSION_SECRET=replace-with-at-least-32-random-bytes
COOKIE_SECURE=false

# log prints login codes to the backend console; resend sends real email.
EMAIL_MODE=log
RESEND_API_KEY=
EMAIL_FROM=Road Rules Trainer <login@driving.domyshev.com>
